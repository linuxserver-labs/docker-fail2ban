# Fail2Ban jail configuration for nginx http 418

[nginx-http-418]

enabled  = false
chain    = DOCKER-USER
port     = http,https
filter   = nginx-http-418
logpath  = %(nginx_access_log)s
maxretry = 10
